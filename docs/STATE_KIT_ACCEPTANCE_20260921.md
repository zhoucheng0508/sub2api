# 官方 0.2.7 + STATE Kit 0.3.4 测试验收

## 版本与环境

- 官方合并提交：`430e43e6e0f8fd8719793aab8d8f2e9ebadc8ee8`，包含官方 main `7c700729c23187d31ed320f6b19c790e2f194826`。
- STATE Kit 接入提交：`203682168f6c`；插件源码固定到 `wangyunjeff/sub2api-state-kit@31420ada`，版本 0.3.4。
- 测试对象为用户指定的独立雨云测试机，Linux amd64，通过 IP 和 HTTP 访问。没有连接或部署 OVH 生产服务器。
- Docker 镜像：`sub2api-custom:0.2.7-statekit-203682168`。
- 实际运行的 amd64 镜像 manifest：`sha256:574c8d7eaac11e6c11d8e2b2820dd5f804294273e0a4e446446aa490b864dec0`。
- 使用新构建的前端与 Linux 后端，再按仓库 `Dockerfile.goreleaser` 打包。容器内 `--version` 确认为 `0.2.7 / 203682168f6c`。
- 镜像传输包 SHA-256：`7665c54a2f2e0eed5370f4052d3defda43c495cf55c227f0a4ac239aababf88a`，传输后再次校验通过。

## 数据与生产隔离

测试机原应用已停止，原 PostgreSQL 和 Redis 正常运行。部署前完成 PostgreSQL custom-format dump、globals、应用配置和文件备份，并验证 `pg_restore --list`。

备份已实际恢复成独立的 `sub2api_statekit_test` 数据库，恢复后的用户、账号、Key 和用量行数与原库一致。应用使用独立 Redis、数据目录与 Compose 项目；新 Nginx server 只响应测试 IP。旧域名转发配置保留，并通过前后 SHA-256 校验。

| 原库记录 | 部署前 | 重启验收后 |
| --- | ---: | ---: |
| users | 50 | 50 |
| accounts | 564 | 564 |
| api_keys | 85 | 85 |
| usage_logs | 300936 | 300936 |

仅在测试副本关闭历史账号调度、历史 Key、定时账号测试、支付提供方、外部通知和第三方登录，并将公开入口地址改为测试地址。原库未执行这些修改。测试库启动后完成迁移，记录数为 295。

备份和回退说明保存在测试机 `/root/sub2api-test-backups/20260921-statekit/`。原应用镜像和原始数据仍保留。停止新增测试 Compose 项目并移走新增 IP server 配置即可撤下本次部署，无须操作生产服务器。

## 已执行验证

### 页面

- 品牌首页、登录页和管理员登录成功。
- 管理控制台展示 0.2.7 和测试站点名称。
- 系统设置页面表单与页签正常，测试 API / 文档入口指向测试机。
- 账号管理正常显示分页表格；分组管理正常显示新增隔离测试分组。
- 插件管理显示 STATE Kit 0.3.4、签名已验证、版本兼容和运行正常。
- 插件配置 iframe 显示“宿主已连接”，账号、分组和代理选择控件加载正常。
- 使用本机关闭端口作为无效代理，手动测试返回“代理测试失败，保留原配置 / 出口 IP 检测失败”。通过 UI 消息接口执行了真实 RunAction。
- 保存配置后验证持久化；验收结束清空无效代理，保持全局 STATE 关闭且账号列表为空。
- 浏览器控制台未观察到 error 级别日志。

浏览器工具对密码输入控件和 iframe 小数坐标操作存在限制，验证改用 placeholder 定位及键盘激活完成，没有修改产品代码绕过浏览器行为。

### 接口、进程与网关

- `/health`、公开配置、登录、当前用户与管理员用户/账号/分组/代理/设置/插件接口均成功。
- 官方 `.s2plugin` 包通过 SHA-256 和 Ed25519 发布者签名校验，上传后状态为 disabled / trusted，再显式启用成功。
- 实际插件状态 `healthy=true`、`host_ready=true`；保存配置自检成功且未触发模型请求。
- 插件默认不开启 STATE 账号；没有发送历史账号的真实模型请求。
- 使用只连本地模拟上游的独立分组、API Key 和 API Key 类型账号测试 Chat Completions。
- 非流式和 SSE 流式均返回 `STATEKIT_TEST_OK`；模型列表包含测试模型。
- 两个初始请求分别记录输入 10、输出 5 Token，标准及实际费用均为 0.0001 USD，流式标志正确。
- 缺少模型返回 HTTP 400，无效 Key 返回 HTTP 401。
- 重启测试应用后健康恢复，STATE Kit 自动启动并重新连接宿主；网关再次成功返回模拟响应。
- 原库行数及旧域名转发配置校验保持一致。

## 本地自动化依据

官方合并阶段：前端定向 361 项通过，后端定向去重 2301 项通过（含子测试）、5 项外部测试按条件跳过；前端类型检查与生产构建、后端普通及 embed 构建通过。详见 `CUSTOMIZATION_DIFF_V0.2.7_CN.md`。

STATE Kit 接入阶段：插件 Go 93 项、插件 UI 44 项、宿主定向 88 项、宿主页面 6 项、真实插件进程契约 13 项通过。常规套件中另一个外部插件集成样例按条件跳过；本次提供临时签名测试包，另行验证真实 STATE Kit 进程。类型检查、相关 ESLint、生产构建与差异检查通过。

## 验证边界

本次验证接入、安装、运行、配置、RPC、页面和网关计费，不宣称改善真实上游模型路由或恢复并发。没有为验收使用真实 OpenAI OAuth 账号或动态代理获取 STATE，也没有操作生产流量。真实票据效果需在专用账号与代理上另行验收。

管理员凭据、数据库凭据、会话令牌、测试 Key、数据库备份及完整运行数据均未进入 Git。
