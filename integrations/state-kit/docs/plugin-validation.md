# 插件版验证记录

## v0.3.4：分组与账号下拉选择（2026-09-20）

- 本地真实浏览器验证：插件停止时也能加载账号目录；选择专用分组只显示对应账号，未分组列表与宿主一致，已添加账号不会重复出现。
- 新账号默认关闭，只添加为表单草稿；切换筛选、刷新状态不保存配置或启动采集，未保存内容在轮询中保留，重新打开页面不会冒充已保存。
- 原有配置和插件关闭状态完整保留，浏览器无脚本异常，窄窗口无横向溢出。本次功能检查不发送模型请求。
- 插件前端 44 项测试通过，新增宿主元数据与原插件页 5 项测试通过；宿主前端类型检查、i18n 检查及完整构建通过。账号目录读取完整分页，仅向 iframe 发送允许的元数据。
- 本版分组选择需配套更新宿主适配；旧宿主继续支持输入账号 ID。原版 0.2.7 的签名安装与进程集成，以及 Linux 发布包验证见本版 Release。

## v0.3.3：新版插件页面重新实测与截图（2026-09-20）

- 通过本地插件页面选择同一个 Pro 账号，提示词「只回复 OK」；先取消「使用当前可用票据」，请求 `gpt-6-astra`，HTTP 200 完整返回 `gpt-5.6-luna`，未注入 STATE。
- 点击该账号「查找票据」，第 2 次动态尝试取得票据并通过账号业务出口复验，状态为可用。
- 勾选「使用当前可用票据」后再次测试，HTTP 200 完整返回 `gpt-6-astra`，明确已注入 STATE、模型匹配。两次测试检测到相同固定业务出口 IP，账号及请求模型一致。
- README 以本轮插件页面的两张截图替换文字表格，成功图在前；账号和 IP 脱敏，模型字段与结果没有改写。真实截图与返回字段逐项核对，浏览器无脚本错误，窄窗口无横向溢出。
- 已保存配置完整保持不变、自动采集仍关闭，只手动启动受测账号；未操作生产环境。没有重复生成鹈鹕或运行糖果测试，原历史 / 失败记录保留并注明日期。

## v0.3.3 发布验证：模型对照与图文说明（2026-09-20）

- 前端 41 项测试、后端 `go test -race ./...` 与 `go vet ./...` 通过。新增请求 / 实际模型对照卡片，覆盖运行中、缺失模型、清空、IP 测试隐藏及模型不符状态。
- HTTP 200 中的上游失败流会明确显示过载、限流或未完成原因；不会标成模型匹配。回归覆盖 `error`、`response.failed` 和 `response.incomplete`，不回显任意上游错误原文。
- 本地适配宿主实际点击快速检查、鹈鹕动画与糖果推理。快速检查和鹈鹕均 HTTP 200 完整返回，实际模型为 `gpt-5.6-luna`、未注入 STATE；HTML / SVG 预览成功。糖果测试在两个账号均遇到上游 `server_is_overloaded`，修正后页面明确提示过载，未取得可评分的回答。
- 五张公开截图来自实际本地界面，仅隐藏账号与代理信息，模型对照字段保留真实结果。窄窗口检查无横向溢出，浏览器无脚本错误；升级保留原配置和所有账号开关，未修改生产服务。
- 最终签名包在 macOS arm64 通过固定官方 Sub2API 0.2.7 安装器及真实插件进程集成：信任 / 篡改拒绝、握手、原始转发、SSE / 取消、账号 / KV、配置及重启。
- 本页展示的完整手动界面需要 v0.3.3 宿主适配和前后端重建；原版 0.2.7 仅基础路径受测。Linux amd64 / arm64 发布验证由仓库工作流检查同一签名包和源码可复现性，并构建适配宿主网页，具体运行链接见 Release。
- README 顶部补充本地插件的历史成功记录：2026-09-20 两个 Pro 账号无票据时返回 Luna，注入票据后均完整返回 `gpt-6-astra`；2026-09-19 的成功鹈鹕 HTML 预览另列。截图只裁取结果区域并脱敏，未修改模型字段。较下方的 Pro / Team 图是用户提供的旧版界面对照，不是本次插件的 Team 实测。

此次发布验证没有取得新的有效 STATE；历史成功对照见下方，不把本次界面和网络测试描述为恢复路由、能力或并发的保证。

## v0.3.3：代理测试到手动查找的会话衔接（2026-09-20，本地修正）

- 修正测试代理后首次手动查找重新抽取会话的问题。成功样本只在内存短时保留，校验网络配置、实际前置代理和有效期；不持久化代理凭据，不混用账号授权或 STATE。
- 单个动态出口检测失败会继续下一次配置内的尝试；授权 401 仍停止。页面区分 IP 检测站连通、账号授权失败、动态出口失败和模型被路由；日志标注是否沿用测试会话。
- 前端 39 项测试及后端 race / vet 通过。回归覆盖真实代理认证会话复用、保存后的衔接、过期及配置变更隔离、有限重试、401 停止和模型不匹配的最终原因。
- 本地 18083 实际点击「测试并应用代理」后，仅启动一个 Pro 测试账号：上方样本与首次查找均检测到同一个美国 IP，日志确认复用了测试会话；HTTP 200 返回 Luna / 312。第 2、3 次动态出口检测失败后仍继续，第 4 次经另一出口得到 HTTP 200 / Luna / 312。本轮没有获取有效票据，不代表模型恢复。
- 原配置完整保留，其余账号没有启动查找，浏览器无脚本异常。真实 IP、账号凭据和截图仅存本地，不进入发布包。

## v0.3.3：三步界面与点击反馈（2026-09-20，本地体验版）

- 前端 37 项测试及后端 race / vet 通过。新增开关失败回退、动作接收与状态更新之间的重复点击保护、停止 / 超时提示、旧账号结果隔离、日志筛选、代理状态失联后的解锁与不保存保障。
- 在本地 18083 的真实浏览器逐项点击：账号开关关闭再开启立即持久化，其他配置保持一致；「测试此账号」正确选中；快速提示词可填入；模型请求运行中输入锁定，停止后显示已取消并恢复按钮。
- 无票据真实模型测试 HTTP 200，返回 Luna；一次单账号重新采集遇到上游 403，账号行与日志如实保留失败。随后模型请求在无新票据时正常放行，完整生成 HTML；页面显示未注入票据，未误报模型恢复。
- HTML / SVG 在隔离 iframe 内可见；切换账号后旧回答隐藏，轮询不会恢复到错误账号。浏览器没有脚本异常。其他旧账号未触发手动查找。
- 本版只更新本地插件体验，保留 Clash 共用前置链路、手动模式与原有账号配置；未发布 GitHub，未修改生产环境。


## v0.3.3：固定业务代理共用前置代理（2026-09-20，本地补充）

- 后端 race / vet 通过，前端 32 项测试通过。真实 TCP 隧道测试覆盖业务出口预检、动态采集、固定出口复验、关闭 STATE 后的转发与无票据基线；未列入插件的账号保持原路线，失效前置代理不静默直连，本机代理桥避免重复串联。
- 本地 18083 开启「账号业务代理也经过此前置代理」，通过 Clash 连接固定代理和动态池。此前两个实验账号均在直连固定代理时超时；修改后两者无票据请求均完成，HTTP 200，实际模型为 `gpt-5.6-luna`，返回 STATE 长度 312。
- 两账号分别首次动态尝试成功，得到 292 票据（检测出口分别为菲律宾、西班牙）；切回各自配置的德国固定代理复验均返回 `gpt-6-astra`。
- 随后在插件网页逐账号执行带 STATE 的模型测试，两次均 HTTP 200、完整结束、明确已注入票据、返回 `gpt-6-astra`，业务出口仍是原德国代理。只证明此次模型路由对照成功，不代表长期有效性或模型能力评分。
- 自动守护继续关闭，其他账号未启动查找。只更新本地实验插件，生产环境未修改；真实账号、代理凭据、票据与截图不进入发布源码。


## v0.3.3：代理自动应用、单账号触发与国家日志（2026-09-20，本地补充）

- 后端 `go test -race ./...`、`go vet ./...` 通过；前端 31 项测试通过。覆盖单账号与批量动作隔离、关闭自动守护后的手动操作、IP 国家查询和缓存、不同轮次日志隔离，以及最终失败记录保留本次出口信息。
- 在本地 18083 的真实插件页面点击「测试并应用代理」，成功显示出口和国家，并自动保存被测试的四个代理字段。核对后端配置，其他设置没有变化；未保存的高级参数草稿仍保留在页面。随后测试不可达代理，失败没有覆盖成功保存的配置。
- 本地关闭自动守护，先后两次通过同一测试账号行的按钮启动采集。持续检查直到任务结束，其余两个账号始终为等待手动查找，没有产生查找任务；页面无脚本错误。单账号与批量操作的区分另有自动化测试覆盖。
- 本次动态尝试实际记录美国、哥伦比亚出口，最终结果均为返回模型不匹配（Luna / 312）；下一次出口检测失败，明确显示「未取得 IP / 国家未知」，没有沿用上一尝试的国家。业务出口检查结果也单独展示。此轮没有取得新的有效票据，不构成恢复模型的成功验证。
- 自动应用保存的是代理配置，动态池仍可能在下一次尝试分配不同出口。没有修改生产环境或发布 GitHub 版本；真实账号、代理配置、截图和运行记录仅保留在本地隔离实例。

## v0.3.3：手动账号操作、测试区与无票据放行（本地验证）

- 后端 race / vet 通过；前端 22 项测试通过。新增无票据放行仍走原业务路径、存在票据继续注入、单账号独立保存不覆盖全局草稿、手动重采绕过冷却、动作去重与取消、IP 请求不携带账号授权、模型回答解析与 HTML 沙箱验证。
- 原版 0.2.7 签名安装与进程集成通过；更新后的宿主适配测试和前端构建通过；适配补丁已从固定官方提交重放成功。
- 通过本地网页实际检测业务出口和动态出口；单账号关闭 STATE 后管理员模型测试 HTTP 200，未注入票据，返回 gpt-5.6-luna；开启后同账号返回 gpt-6-astra，已注入票据，完整生成鹈鹕骑自行车 HTML，页面内成功显示 SVG 和动画脚本。
- 网页「立即重新找票据」动作成功进入重新采集，仍有效的旧票据可继续使用。
- 在隔离实例中临时使用不可达动态代理，确认采集失败且没有可用票据后，通过实际 Base URL / Key 请求得到 HTTP 200、完整 Luna 响应；验证了放行行为，不把它误报为恢复目标模型。测试后已恢复原动态池和配置。
- 最终页面启动检查确认默认选中账号，初始化完成前测试按钮保持禁用；旧票据到期后实测仍正常返回完整 HTML 和实际模型（Luna），页面明确显示未注入票据及模型不匹配。
- 浏览器验证预览允许内联脚本，不能读取父框架 DOM，外部 fetch 被 CSP 阻止。生产环境未操作；本轮没有 Team 实测。


## v0.3.2：账号名称和三种前置代理方式（2026-09-19）

- 插件 `go test -race ./...`、`go vet ./...` 通过；前端 19 项测试通过。新增原版宿主回退、名称安全显示、未保存输入保留、代理 ID 保存/解析、失效代理不直连兜底测试。
- 可选宿主适配的能力限制、目录过滤、账号名称、过期/停用代理排除、代理认证实时更新及错误脱敏测试通过。适配补丁已从官方固定提交在独立目录重放成功。
- 使用原版 0.2.7 的真实签名安装器和插件进程，通过信任链、二进制转发、SSE/取消、账号与 KV broker、旧宿主不支持新 RPC 时的运行回退、进程重启测试。
- 本地隔离实例（macOS arm64，适配宿主）通过实际网页依次保存直连、手动、IP 管理三种方式；网页显示账号名称与 ID，浏览器无脚本错误。最终使用 IP 管理中的前置代理。
- 同一 Pro 账号通过所选前置代理连接动态池，第 4 次采集返回目标模型和 292 字节 STATE；原业务代理复验也返回目标模型。随后实际通过 Base URL / 测试 Key 调用，HTTP 200，完整响应模型为 `gpt-6-astra`。

账号名称和代理下拉的验证基于适配宿主，不能表述为未修改的官方 0.2.7 原生具备。没有修改生产服务；没有针对 Team 或多副本开展本次实测。下面保留历史版本验证记录。


## v0.3.1：前置代理、出口 IP 与运行日志（2026-09-19）

本版保留原有账号开关、套餐选择、票据采集/复验/持久化与守护逻辑，增加可选前置代理和采集日志。未修改官方宿主，也不读取宿主「代理管理」列表。

- 后端 35 个顶层测试（含子用例）的 race 与 vet 通过：HTTP/SOCKS 双层代理组合、HTTPS 上游、前置认证隔离、取消释放、拒绝后不回退直连、固定业务代理隔离、IP 查询不含 OAuth / STATE、IP 检测失败不阻断采集、日志容量与脱敏、配置指纹兼容。
- 网页 17 项测试通过：新增前置代理和出口检测开关保存、只读刷新保留未保存输入、日志显示和敏感字段过滤；保留原 Bridge 来源校验。
- 官方 0.2.7 安装器和真实插件进程集成通过：签名校验、安装、配置、HostService、KV、转发、取消、停止与重启。
- 本地独立官方 0.2.7 实例实际安装并启用；在真实浏览器的宿主 iframe 内检查配置、保存开关和查看日志，无页面异常。
- 真实测试使用用户授权的同一 Pro 账号、同一目标模型与同一个原业务出口。第一轮采集遇到 Luna / 312 和 403，按规则冷却；第二轮第 2 次获得 292 票据，通过原业务出口复验后才发布使用。
- 用本地 Sub2API 的 Base URL / API Key 发请求，并通过真实网页切换开关：**关闭 → HTTP 200 / Luna / 312；开启 → HTTP 200 / gpt-6-astra；再次关闭 → Luna / 312；再次开启 → gpt-6-astra**。所有业务响应均完整结束。本地账号与前置/动态/业务代理配置未放入仓库。
- 上传最终签名包后，插件从 Redis 恢复票据，通过原业务出口重新核验，状态回到可用；到期时间保持原值，没有因重启延长有效期。
- [Linux 原生 CI 35441828242](https://github.com/wangyunjeff/sub2api-state-kit/actions/runs/35441828242)：**amd64 / arm64 全部通过**，包括 race、网页测试、待发布签名包验证、源码与包内二进制逐字节比对、官方 0.2.7 安装器与真实进程集成。运行源码提交 `c8082411a8001b2f6f21c06e491a4d2dea6f387c`。

这次成功仅证明受测账号在本次短时对照中恢复了目标模型响应，没有测试真实 Team、模型能力、长达一小时持续有效或并发提升；不保证其他账号或以后每次采集都成功。

出口 IP 来自同一代理会话上的独立 ipify 查询，不能证明每条模型请求的精确源 IP。运行日志只保留结构化元数据，不记录代理密码、账号 Token 或票据原文。默认前置代理为空，出口检测关闭，正常业务请求不会新增 IP 查询。公开源码和包不含本地真实配置与运行日志。

## v0.3.0 历史验证

对象：STATE Kit 插件 `0.3.0`，官方 Sub2API `v0.2.7`（`aea725f2ea644d5592d0bbb1d63b607efa7e200a`）。插件源码、UI 和发布者签名均属于本项目；没有运行或复制用户提供的私有传输插件。

## 验证层次

1. **后端自动化：** 默认关闭、账号隔离、Pro / Team 筛选、采集与固定出口复验、持久化恢复、失败续期保留旧票据、异常守护、取消与并发。
2. **网页自动化：** 沙箱消息来源和 Token 校验、超时、加载/保存、账号默认关闭、参数校验、只读状态刷新、未保存修改保留及敏感字段遮挡。
3. **官方宿主集成：** 使用官方安装器加载真正的签名 `.s2plugin`，启动独立进程，与宿主通过 gRPC / HostService 通信，再转发本地可控上游。
4. **原生 Linux：** CI 在 amd64 和 arm64 上下载待发布的同一个签名包，验证哈希/签名，与当前源码的可复现构建比对，并运行官方宿主集成。

## 本地执行结果（2026-09-19）

- `go test -race ./...`：通过；覆盖 27 个后端顶层测试及其子用例。
- `go vet ./...`：通过。
- `node --test ui-tests/*.test.cjs`：15 项通过。
- 官方 0.2.7 宿主安装器及真实子进程集成：在 macOS arm64 上通过，包括未知发布者、篡改载荷和篡改签名拒绝；原始转发、SSE 逐帧传递/取消、反向账号目录与 KV、启动/关闭/重启。
- 网页：在真实浏览器的 `sandbox="allow-scripts"` 模拟宿主里验证添加账号、Pro/Team 选择、保存，以及自动轮询保留未保存输入。修复了沙箱禁止原生表单提交造成的保存失效问题。页面使用模拟账号 ID，没有真实账号或代理。
- 隐私：新增源码、UI、说明与已提供的 6 份私有账号/代理导出进行实际值比对，未匹配到其中的敏感字段。发布只收集明确允许的源码/静态资源和本次编译程序。

## 原生 Linux 结果

[GitHub Actions run 35424128685](https://github.com/wangyunjeff/sub2api-state-kit/actions/runs/35424128685)：Linux **amd64 与 arm64 全部通过**。

每种架构均完成 race 与网页测试，下载同一个待发布签名包，验证发布者签名及所有文件 SHA256，将公开源码重新编译后与包内二进制逐字节校验，再通过官方 0.2.7 安装器和真实插件进程执行上述集成测试。

三种平台受测插件包的 SHA256：

```text
653bc340c564672b3f18feec84a271ee0eaf837cbf886c6df8e617c2d6c3a9ee
```

自动化验证源码提交为 `ef29a56c0971f9727cd63b66542a640da9d73545`；后续验证记录更新不改变插件程序或网页载荷。

## 不包含在这轮验证中的内容

- 没有使用作者的真实 OAuth 账号、代理池或生产服务验证这次插件是否恢复了模型路由。
- 控制上游模拟了成功、模型不符和异常 STATE；测试通过证明本地采集/验证/注入逻辑按这些输入工作，不证明上游一定返回可用票据。
- 没有生产流量压测、长达一小时的真实上游有效期保证或多副本协同结论。
- 宿主接口只支持独立插件配置页；不会自动迁移源码版的账号设置、票据，也不会把控件插进原账号编辑页。

用户原有的真实测试实例与生产环境不参与上述隔离测试。正式包默认 STATE 关闭、账号配置为空；不打包私有运行数据或签名私钥。
